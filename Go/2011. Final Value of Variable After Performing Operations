func finalValueAfterOperations(operations []string) int {
    count := 0
    for _, v := range operations{
        if v == "X--" || v == "--X"{
            count--
        }else{
            count++
        }
    }
    return count
}
