import "sort"
func containsDuplicate(nums []int) bool {
    sort.Ints(nums)
    for i:=0; i<len(nums); i++{
        if i+1< len(nums) && nums[i]==nums[i+1]{
            return true
        }
    }
    return false
}
